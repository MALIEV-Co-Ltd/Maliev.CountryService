apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    run: maliev-countryservice-api
  name: maliev-countryservice-api
  namespace: maliev
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    name: http
  selector:
    run: maliev-countryservice-api
  sessionAffinity: None
  type: NodePort
